What is the difference between source() and ref()?
Source is for raw external data, while ref is for other dbt models.

Why are staging models typically views and marts typically tables?
Staging models typically just refelect their source tables while marts need to have clean transformed data.

What error message did the circular reference produce?
Found a Cycle