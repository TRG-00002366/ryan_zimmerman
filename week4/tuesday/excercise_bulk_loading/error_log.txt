Got the error SQL compilation error: error line 80 at position 4 invalid identifier 'ROWS_PARSED'
for the task6 sql query
-- Check CSV load history
SELECT 
    TABLE_NAME,
    FILE_NAME,
    STATUS,
    ROWS_PARSED,
    ROWS_LOADED,
    ERRORS_SEEN,
    FIRST_ERROR
FROM TABLE(INFORMATION_SCHEMA.COPY_HISTORY(
    TABLE_NAME => 'RAW_PRODUCTS',
    START_TIME => DATEADD('hour', -1, CURRENT_TIMESTAMP())
));

-- Check JSON load history
SELECT 
    TABLE_NAME,
    FILE_NAME,
    STATUS,
    ROWS_PARSED,
    ROWS_LOADED,
    ERRORS_SEEN
FROM TABLE(INFORMATION_SCHEMA.COPY_HISTORY(
    TABLE_NAME => 'RAW_CUSTOMER_EVENTS',
    START_TIME => DATEADD('hour', -1, CURRENT_TIMESTAMP())
));


Resolution: Invalid column names, I replace the with the correct names.

SELECT 
    TABLE_NAME,
    FILE_NAME,
    STATUS,
    ROW_PARSED,
    ROW_COUNT,
    ERROR_COUNT,
    FIRST_ERROR_MESSAGE